/**
 * The `ActivityMain` class serves as the entry point for the activity management application.
 * It initializes and starts the main application by calling the `initApp` method from the `ActivityManagement` class.
 */
package com.u2team.huansync.activity;

public class ActivityMain {

    /**
     * The main method that serves as the entry point for the application.
     * It initializes and starts the activity management application by calling the `initApp` method.
     *
     * @param args Command line arguments (not used in this application).
     */
    public static void main(String[] args) {
        ActivityManagement.initApp();
    }
}
